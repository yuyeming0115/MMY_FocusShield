<Window x:Class="FocusShield.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="FocusBar" 
        Height="120" Width="10" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseMove="Window_MouseMove"
        MouseDoubleClick="Window_MouseDoubleClick"
        MouseRightButtonDown="Window_MouseRightButtonDown">

    <Window.ContextMenu>
        <ContextMenu Name="TrayMenu">
            <MenuItem Name="MenuToggle" Header="🔒 关闭屏蔽" Click="MenuToggle_Click"/>
            <MenuItem Header="⚙️ 设置..." Click="MenuSettings_Click"/>
            <Separator/>
            <MenuItem Header="❌ 退出" Click="MenuExit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Grid>
        <Border x:Name="GlowBorder" 
                Background="#FFFF0000"
                CornerRadius="5"
                HorizontalAlignment="Stretch" 
                VerticalAlignment="Stretch">
            <Border.Effect>
                <DropShadowEffect x:Name="GlowEffect" Color="Red" BlurRadius="15" ShadowDepth="0" Opacity="1"/>
            </Border.Effect>
        </Border>
    </Grid>
</Window>
