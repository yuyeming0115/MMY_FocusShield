<Window x:Class="FocusShield.ProcessPickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="选择应用程序"
        Width="480" Height="420"
        MinWidth="400" MinHeight="300"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        WindowStyle="ToolWindow"
        Background="#F9F9F9">

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 搜索框 -->
        <TextBox Grid.Row="0"
                 Name="TxtSearch"
                 Height="32"
                 Margin="0,0,0,8"
                 VerticalContentAlignment="Center"
                 TextChanged="TxtSearch_TextChanged">
            <TextBox.Resources>
                <Style TargetType="TextBox">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TextBox">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="#CCC"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="8,0,0,0">
                                    <Grid>
                                        <TextBlock Name="Placeholder"
                                                   Text="搜索进程名或窗口标题..."
                                                   Foreground="#AAA"
                                                   VerticalAlignment="Center"
                                                   Margin="4,0,0,0"
                                                   IsHitTestVisible="False">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding Text, RelativeSource={RelativeSource TemplatedParent}}" Value="">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ScrollViewer x:Name="PART_ContentHost"/>
                                    </Grid>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </TextBox.Resources>
        </TextBox>

        <!-- 进程列表 -->
        <ListView Grid.Row="1"
                  Name="LstProcesses"
                  Margin="0,0,0,8"
                  SelectionMode="Single"
                  MouseDoubleClick="LstProcesses_MouseDoubleClick">
            <ListView.View>
                <GridView>
                    <GridViewColumn x:Name="ColIcon" Width="36">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Image Source="{Binding Icon}"
                                       Width="16" Height="16"
                                       Stretch="Uniform"/>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn x:Name="ColName" Header="进程名" Width="120"
                                   DisplayMemberBinding="{Binding ProcessName}"/>
                    <GridViewColumn x:Name="ColTitle" Header="窗口标题" Width="280"
                                   DisplayMemberBinding="{Binding WindowTitle}"/>
                </GridView>
            </ListView.View>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Padding" Value="4,2"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <!-- 按钮栏 -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Name="BtnAdd"
                    Content="添加"
                    Width="80" Height="32"
                    Margin="0,0,8,0"
                    Background="#222" Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="BtnAdd_Click"
                    IsDefault="True">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            <Button Name="BtnCancel"
                    Content="取消"
                    Width="80" Height="32"
                    Background="#EEE" Foreground="#333"
                    BorderBrush="#DDD"
                    Cursor="Hand"
                    Click="BtnCancel_Click"
                    IsCancel="True">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
